#!/usr/bin/env ruby
libdir = File.expand_path("../../lib", File.symlink?(__FILE__) ? File.readlink(__FILE__) : __FILE__); $LOAD_PATH.unshift(libdir) unless $LOAD_PATH.include?(libdir)

require 'rails_wizard/command'

RailsWizard::Command.start
